[Unit]
Description=OpenRGB SDK Server Daemon


[Service]
Type=simple
Environment="QT_QPA_PLATFORM=offscreen"
ExecStart=/usr/bin/openrgb --server --noautoconnect --startminimized
Restart=always
RestartSec=2

[Install]
WantedBy=default.target
